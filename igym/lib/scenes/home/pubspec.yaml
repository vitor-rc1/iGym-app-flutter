name: home
description: "A new Flutter module project."

version: 1.0.0+1

publish_to: none

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  design_system:
    path: ../../design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.home
    iosBundleIdentifier: com.example.home
